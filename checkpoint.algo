 var voy = str.voyAt(i).
  if (
    voy === "a" ||
    voy === "e" ||
    voy === "i" ||
    voy === "o" ||
    voy === "u"
  )
  {
    vowels ++;
  }
  {
console.log("Longueur de la phrase : " + length);
console.log("Nombre de mots dans la phrase : " + words);
console.log("Nombre de voyelles dans la phrase : " + vowels);


{
    vowels++;
  }
}

console.log(length);
console.log(words);
console.log(vowels);